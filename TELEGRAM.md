# Як підключити Telegram, щоб приходили заявки

Інструкція для новачка. Займе приблизно 5 хвилин. Нічого програмувати не треба —
потрібно лише вставити два рядки у файл `script.js`.

---

## Крок 1. Створити бота

1. Відкрийте Telegram і в пошуку зверху знайдіть **@BotFather**
   (у нього синя галочка — це офіційний бот Telegram).
2. Натисніть **Start** (Почати).
3. Надішліть команду:
   ```
   /newbot
   ```
4. BotFather запитає **назву бота** — напишіть будь-що, наприклад:
   ```
   GalaxPlus Заявки
   ```
5. Далі запитає **username бота** — має бути унікальним і закінчуватись на `bot`,
   наприклад:
   ```
   galaxplus_zayavky_bot
   ```
   Якщо ім'я зайняте — просто додайте цифри.
6. У відповідь BotFather надішле повідомлення з **токеном**. Виглядає приблизно так:
   ```
   7123456789:AAEhBOweik6ad9r_ZxK3vAbcDefGhIjKlMn
   ```

**Збережіть цей токен** — він знадобиться на кроці 3.

> Токен — це пароль від бота. Нікому його не пересилайте.

---

## Крок 2. Дізнатися свій Chat ID

Chat ID — це номер вашого акаунту, щоб бот знав, кому надсилати заявки.

1. У Telegram знайдіть свого щойно створеного бота (за username з кроку 1)
   і натисніть **Start**. **Це обов'язково** — без цього бот не має права вам писати.
2. Тепер у пошуку знайдіть бота **@userinfobot** і натисніть **Start**.
3. Він одразу надішле ваші дані. Потрібен рядок **Id**, наприклад:
   ```
   Id: 123456789
   ```

**Збережіть це число.**

---

## Крок 3. Вставити дані у файл

1. Відкрийте файл `script.js` (у вашому репозиторії на GitHub натисніть на файл,
   потім на олівець «Edit this file» вгорі справа).
2. Знайдіть угорі блоку налаштувань ці два рядки:
   ```js
   const TELEGRAM_BOT_TOKEN = '';
   const TELEGRAM_CHAT_ID   = '';
   ```
3. Вставте свої значення **між лапками**:
   ```js
   const TELEGRAM_BOT_TOKEN = '7123456789:AAEhBOweik6ad9r_ZxK3vAbcDefGhIjKlMn';
   const TELEGRAM_CHAT_ID   = '123456789';
   ```
4. Внизу сторінки натисніть **Commit changes**.

Netlify автоматично оновить сайт протягом ~30 секунд.

---

## Крок 4. Перевірити

1. Відкрийте сайт, натисніть на будь-яку картку в блоці «Що ми робимо».
2. Заповніть форму та натисніть «Надіслати заявку».
3. Протягом кількох секунд у Telegram має прийти повідомлення:

```
🔔 Нова заявка з сайту GalaxPlus

📌 Послуга: Генеральний підряд
👤 Ім'я: Іван
📞 Телефон: +380671234567
💬 Коментар: Потрібен розрахунок

🕒 03.08.2026, 14:22
```

---

## Якщо заявки не приходять

**Перевірте, що ви натиснули Start у свого бота** — це найчастіша причина.
Telegram забороняє ботам писати першими.

**Перевірте лапки.** Токен і ID мають бути в лапках, кома/крапка з комою на місці.

**Відкрийте консоль браузера** (клавіша F12, вкладка Console) — якщо там написано
`Telegram не налаштовано`, значить рядки залишились порожніми.

---

## Заявки на кількох людей

Якщо потрібно, щоб заявки бачила вся команда:

1. Створіть у Telegram **групу** та додайте туди потрібних людей.
2. Додайте в групу свого бота (Add member → знайдіть за username).
3. Напишіть у групі будь-яке повідомлення.
4. Відкрийте у браузері (замість `ВАШ_ТОКЕН` вставте свій токен):
   ```
   https://api.telegram.org/botВАШ_ТОКЕН/getUpdates
   ```
5. Знайдіть у відповіді `"chat":{"id":-1001234567890`.
   Число **разом зі знаком мінус** — це ID групи.
6. Вставте його у `TELEGRAM_CHAT_ID` замість особистого ID.

---

## Важливо знати про безпеку

Токен бота у файлі `script.js` видно будь-кому, хто відкриє код сайту.
Для збору заявок це прийнятний ризик: максимум, що зможе зробити зловмисник —
надсилати вам спам-повідомлення в цей чат. Доступу до вашого акаунту токен не дає.

Якщо колись знадобиться сховати токен — це робиться через Netlify Functions.
Скажіть, і я допоможу налаштувати.
